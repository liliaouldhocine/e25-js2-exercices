// 2. Callback avec Méthodes de Tableau
// Objectif : Utiliser des callbacks avec map ou filter.

// Exercice :
// Utilisez map pour doubler chaque élément d'un tableau, en passant une callback.

// javascript
const nombres = [1, 2, 3];
const doubler = (x) => x * 2;

// Utilisez map avec la callback 'doubler'
const resultat = nombres.map(/* Votre code ici */);
console.log(resultat); // [2, 4, 6]
