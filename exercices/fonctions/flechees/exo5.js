// Objectif : Maîtriser le retour implicite (sans {} et return).

// Exercice :
// Simplifiez ces fonctions fléchées en utilisant le retour implicite.

// 1.
const square = (x) => {
  return x * x;
};

// 2.
const makeObject = (value) => {
  return { key: value };
};
