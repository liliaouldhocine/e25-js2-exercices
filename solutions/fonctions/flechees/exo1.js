// 1.
const sayHello = () => "Hello!";

// 2.
const double = (x) => x * 2; // Parenthèses optionnelles pour 1 paramètre

// 3.
const add = (a, b) => a + b;

// 4.
const greet = (name) => `Bonjour, ${name}!`;
